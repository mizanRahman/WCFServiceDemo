== WCF Web Service Demonstration.

creating and Using WCF Web Service is not hard.

Start up Rails and visit /index to get the modified homepage (should be visually identical,
just reoriented for dynamic web bits).

==components

WCF Service (IIS hosted)
Service consumer
Service proxy

== Requirements

.NET
IIS

== Setup

host the wcf service to IIS and run the consumer application.